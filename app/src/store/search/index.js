const actions = {
    // 异步获取token,并存储到state中
}
const mutations = {
    // 直接修改token的值
}
const state = {
    // 存储token
}
const getters = {
    // 对token进行加工后返回
}

export default {
    actions,
    mutations,
    state,
    getters
}